# Project Instructions for Claude Code

**Before making any changes, read `PROJECT_DOCUMENTATION.md` first.** It is this repo's continuously maintained source of truth: project overview, current status, the full error/fix log, key decisions, setup/run instructions, and known gotchas. A new session should not need this conversation's history to understand the project — that file substitutes for it.

**After completing any meaningful task, error fix, or feature, update `PROJECT_DOCUMENTATION.md` in the same session, before finishing.** Do not wait to be asked. Cover: what changed, why (root cause for fixes), current status, and anything a future session should watch out for.

Do not write a live credential (password, token, key) into `PROJECT_DOCUMENTATION.md` or any other tracked file — describe how to obtain access instead.
